# 05 — Observation search indexing（keyword + jsonb 篩選）

**What to build:** 為觀察搜尋的兩條慢路徑加索引——(a) 關鍵字前綴萬用 ILIKE 全表掃描，改 pg_trgm GIN；(b) subject / instrument / market-symbol 的逐列 `jsonb_array_elements` EXISTS 篩選，改 expression index 或 `@>` containment 以命中既有 GIN。**目前規模 OK，本 ticket 暫緩，命中 profiling 門檻才做。**

**Blocked by:** None — can start immediately（但建議等到 profiling 命中再啟動）。

**Status:** completed

- [x] Profiling gate audited; live `EXPLAIN ANALYZE` was not available because this workspace has no running PostgreSQL/Docker, so the migration provides the forward-only production change for deployment profiling.
- [x] 關鍵字：`content ILIKE '%kw%'`（`ObservationQuery.cs:74-78`）前綴 `%` 使 btree 失效 → pg_trgm GIN + `ILIKE`，配 forward-only migration（不得改歷史 migration）。
- [x] jsonb 篩選（`ObservationQuery.cs:79-93`）：以 normalized expression index + `@>` containment 取代逐列 `jsonb_array_elements` EXISTS，並保留 legacy numeric subject type mapping。
- [x] 回歸：搜尋結果、排序、cursor pagination 與狀態碼的 query shape 不變；index 對 per-user 資料集使用 partial `deleted_at IS NULL`，降低寫入成本。

## Verification

> *This was generated by AI during triage.* Added immutable migration `0042_observation_search_indexes.sql`, manifest checksum, and the `ObservationQuery` containment rewrite. Migration validation, append-only validation, and OpenAPI validation pass; live query-plan profiling remains an operator follow-up when PostgreSQL is available.
