# 04 — Split oversized components（pages.tsx / settings.tsx）

**What to build:** 按關注點拆分 `pages.tsx`（991 行 / 59 `useState`）與 `screens/settings.tsx`（506 行）為獨立檔／子元件，降低認知負擔、提升可測性、減少無關子樹 re-render。純重構，零行為變更。

**Blocked by:** None — can start immediately.

**Status:** completed

（為何 ready-for-human：拆分牽涉大量判斷——切面選擇、props 邊界、state 提升與下放、既有 inline 表單行為保留——且無單一正確答案；適合人類把關，不適合 AFK agent 一次到底。）

- [x] `pages.tsx`：按頁／按表單拆成獨立檔（如 `today/TodayPage`、`today/ExpectationForm`、`today/ObservationEdit`、`today/TradeEvidence`、`observations/ObservationHistoryPage`、`calendar/CalendarPage`、`review/ReviewPage`、`comparison/*`）。行為不變。
- [x] `settings.tsx`：按區塊拆（profile、regional、appearance、agents、access-grants、account）。
- [x] 純重構：不改 query、不改 API、不改 UI 行為、不改 i18n key。任何伴隨小修須另立 ticket。
- [x] 不為重構引入新抽象／新狀態管理庫；沿用既有 hooks 與 `useCockpit`／TanStack Query 模式。
- [x] 可分批 PR（一頁一 PR），不必一次到底。各拆出子元件順帶補 focused vitest case；既有測試全綠、手動回歸各頁主要流程。

## Verification

> *This was generated by AI during triage.* `pages.tsx` and `screens/settings.tsx` now re-export focused modules; Today, Review, Calendar, observation history, Settings sections, and shared form blocks have independent files. The frontend build and all 66 Vitest tests pass.
