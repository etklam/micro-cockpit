# Agent working area

This directory contains temporary PRDs, tickets, and verification notes produced during earlier work. Status labels inside those files describe their original workflow state; they do not make a file a current product requirement.

The normative product sources are `PRODUCT.md` and `CONTEXT.md`. The active plan is `ROADMAP.md`. Reassess every scratch item against those files and the current repository before implementation.
