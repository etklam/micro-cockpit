apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: micro-cockpit
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
