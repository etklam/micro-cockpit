# Archived planning documents

Files in this directory preserve historical product and implementation planning. They are not active requirements.

For current direction, use `PRODUCT.md`, `CONTEXT.md`, and `ROADMAP.md` at the repository root. When an archived plan conflicts with those files, the root sources take precedence.
