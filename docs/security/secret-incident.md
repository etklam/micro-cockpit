# Secret incident record

This record intentionally contains metadata only. Credential values must always be represented as `[REDACTED]`.

| Credential category | Affected file paths | Affected services | First known commit | Required rotation action | Rotation verification status | History-removal status |
| --- | --- | --- | --- | --- | --- | --- |
| PostgreSQL administrator password | `k8s/01a-db-secret.yaml`, `k8s/03-postgres-deployment.yaml` | PostgreSQL | `0b62b3429604bc69b5fd007a204996cc60c10856` | Back up the database, run `ALTER ROLE` through the rotation workflow, update `db-credentials`, and verify the role verifier changed and the new login works | Pending operator cluster access | Pending coordinated history rewrite |
| Migrator database password | `k8s/01a-db-secret.yaml`, `k8s/05-db-init-job.yaml` | Database initialization and migration operator | `76505ded8d382c6993da6de539799effa16db542` | Rotate `trade_diary_migrator`, update `db-credentials`, and verify the new role connection | Pending operator cluster access | Pending coordinated history rewrite |
| Per-service database passwords | `k8s/01a-db-secret.yaml`, `k8s/05-db-init-job.yaml`, `k8s/06-services.yaml` | Identity, Journal, Performance, Discipline, Reminder, Stock Research, Market Data, Price Alert, Rotation, Partner, Content, Operations | `76505ded8d382c6993da6de539799effa16db542` | Rotate every service LOGIN role, update `db-credentials` and `service-connection-strings`, roll out each service, and verify each runtime role connection | Pending operator cluster access | Pending coordinated history rewrite |
| Local registration key | `k8s/01b-app-secret.yaml`, `k8s/06-services.yaml` | Identity | `0b62b3429604bc69b5fd007a204996cc60c10856` | Generate a new key, update `app-secrets`, roll out Identity, and verify readiness | Pending operator cluster access | Pending coordinated history rewrite |
| Internal service key | `k8s/01b-app-secret.yaml`, `k8s/06-services.yaml` | Journal, Reminder, Market Data, Price Alert, Operations | `cbcc09a539f2b8bea51b5528bd64eaafb16a64b9` | Generate a new key, update `app-secrets`, roll out all producers and consumers together, and verify readiness | Pending operator cluster access | Pending coordinated history rewrite |
| Shared local-development PostgreSQL fallback | Service `Program.cs` files and Rotation `appsettings.json` / `appsettings.Development.json` | Identity, Journal, Performance, Discipline, Reminder, Stock Research, Market Data, Price Alert, Rotation, Partner, Content, Operations | `cbcc09a539f2b8bea51b5528bd64eaafb16a64b9` | Rotate the affected PostgreSQL login anywhere this fallback was used; require connection strings from environment or a local ignored secret file | Pending operator verification for any environment that used the fallback | Pending replacement during coordinated history rewrite |
| Smoke-test authentication and internal-call credentials | Eight `tests/smoke-*.sh` scripts | Local Identity test users and local internal service callers | `cbcc09a539f2b8bea51b5528bd64eaafb16a64b9` | Revoke or rotate values in any non-isolated environment where they were accepted; supply test credentials from ignored operator environment variables | Pending operator verification; current scripts contain no literal values | Pending replacement during coordinated history rewrite |

## Audit scope and outcome

The current tree and all reachable Git commits were inspected using metadata-only `git grep`, `git log -p`-equivalent history traversal, and the repository validator. Kubernetes manifests, both CI systems, environment templates, Compose, documentation, service configuration, and shell scripts were included. References to registry, SSH, and CI credentials already resolve from action secret stores and were not classified as exposed values. The Identity RSA signing key was excluded because it was not part of this plaintext incident.

Implementation status: completed. Kubernetes Secrets are the production runtime credential source of truth. Normal Forgejo releases contain deployment infrastructure credentials only, verify the existing runtime Secrets without changing them, and share one namespace operation lock with recoverable rotation. Bootstrap replacement requires explicit operator confirmation. These controls, old-credential negative verification, pinned deployment-host trust, immutable release tags, and rollback paths are covered by static and mocked failure-path tests.

Live rotation status: pending operator execution. No exposed credential is claimed revoked without cluster evidence. Run `scripts/rotate-k8s-credentials.sh` only after recording a verified backup reference and confirming the intended Kubernetes context and namespace.

History rewrite status: pending. Rewriting history remains a separate coordinated operator action after live rotation evidence exists.

Database migration implementation status: completed with static, mocked Kubernetes, and ephemeral PostgreSQL verification. Production baseline and live Kubernetes migration Job verification remain pending operator execution. Credential rotation and Git history rewrite remain pending; no live completion is claimed.
